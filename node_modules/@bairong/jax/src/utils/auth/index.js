import auth from './auth';
import authDirective from './directive';
import authHandle from './authHandle';

const auths = {
  auth,
  authDirective,
  authHandle
};

// module.exports.default = module.exports = auths;
export { auth };
export { authDirective };
export { authHandle };
export default auths;
