import Vue, { VNode } from 'vue';
import { PluginFunction, PluginObject } from 'vue';
export class http {
  get(param: object): object;
  post(param: object): object;
  LOGIN_PAGE: string;
  DOMAIN: string;
  SESSION_KEY: string;
}

export interface auth extends PluginObject<any> {

}
export interface authRouter extends PluginObject<any> {

}
export interface authDirective extends PluginFunction<{}> {
  setAuthTree(param: []): Function;
}
export interface formatDate {

}
export interface regExp {

}

export interface authHandle {

}

export interface resourceConf {
  layoutLogoURL: string
  layoutMinLogoURL: string
}